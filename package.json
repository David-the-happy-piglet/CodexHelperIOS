{
  "name": "codex-companion-for-iphone",
  "private": true,
  "workspaces": [
    "packages/protocol",
    "desktop-helper"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces"
  }
}

